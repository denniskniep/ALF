cohorts:
- name: test
  features_config: default
  fields: []
  weight: 1.0
  warmup_count: 3
  model:
    name: NoOp
database:
  type: memory
features:
  default:
    message:
      type: str_text
      preprocessor:
        name: SentenceTransformerEncoder
        params:
          embed_dim: 2
