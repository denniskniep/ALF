cohorts:
- name: test
  features_config: default
  fields: []
  weight: 1.0
  model:
    name: NoOp
database:
  type: memory
features:
  default:
    browser:
      type: str_categorical
      preprocessor:
        name: HashIndex
        params:
          n_features: 4
